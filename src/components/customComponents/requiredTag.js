import React from "react";
import { View, Text } from "react-native";

const RequiredTag = () => {
  return (
    <Text style={{ fontSize: 15, fontFamily: "MB", color: "red" }}>*</Text>
  );
};

export default RequiredTag;
